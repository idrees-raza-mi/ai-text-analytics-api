===============================================
AI TEXT ANALYTICS API - COMPLETE SETUP GUIDE
===============================================

📋 SYSTEM REQUIREMENTS:
- Windows 10/11
- Python 3.8 or higher
- Internet connection (for package installation)
- 2GB+ free disk space

🔧 DEPENDENCIES NEEDED:

Core Dependencies (Essential):
- fastapi          # Web framework for API
- uvicorn          # ASGI server
- pydantic         # Data validation
- python-multipart # File upload support
- requests         # HTTP client
- python-dotenv    # Environment variables
- textblob         # Natural language processing
- langdetect       # Language detection
- numpy            # Numerical computing
- scikit-learn     # Machine learning tools

📥 INSTALLATION STEPS:

1. VERIFY PYTHON INSTALLATION:
   Open PowerShell and run:
   python --version
   
   (Should show Python 3.8+. If not installed, download from python.org)

2. NAVIGATE TO API FOLDER:
   cd C:\Users\hp\ai-text-analytics-api

3. INSTALL DEPENDENCIES:
   pip install fastapi uvicorn pydantic python-multipart requests python-dotenv textblob langdetect numpy scikit-learn

4. DOWNLOAD NLTK DATA (Required for TextBlob):
   python -c "import nltk; nltk.download('punkt'); nltk.download('brown')"

🔐 CONFIGURATION:

1. VERIFY API KEY:
   Check .env file contains:
   API_KEY=SecureAI2024_TextAnalytics_789xyz

2. YOUR API KEY IS: SecureAI2024_TextAnalytics_789xyz
   (You'll need this to test the API)

🚀 STARTING THE API:

Method 1 - Simple Start:
uvicorn main:app --host 0.0.0.0 --port 8000

Method 2 - With Reload (for development):
uvicorn main:app --host 0.0.0.0 --port 8000 --reload

Method 3 - Run Python file directly:
python main.py

🌐 ACCESSING THE API:

Once started, your API will be available at:
- Main API: http://localhost:8000
- Documentation: http://localhost:8000/docs
- Alternative docs: http://localhost:8000/redoc
- Health check: http://localhost:8000/health

🧪 TESTING THE API:

1. Health Check (No API key needed):
   Open browser: http://localhost:8000/health
   Should show: {"status": "healthy", "message": "API is running"}

2. Test Sentiment Analysis (PowerShell):
   $headers = @{ "X-API-Key" = "SecureAI2024_TextAnalytics_789xyz"; "Content-Type" = "application/json" }
   $body = '{"text": "I love this product!"}'
   Invoke-RestMethod -Uri "http://localhost:8000/analyze-sentiment" -Method Post -Headers $headers -Body $body

3. Using curl (if available):
   curl -X POST "http://localhost:8000/analyze-sentiment" ^
        -H "X-API-Key: SecureAI2024_TextAnalytics_789xyz" ^
        -H "Content-Type: application/json" ^
        -d "{\"text\": \"This is amazing!\"}"

📍 API ENDPOINTS:

All endpoints require API key in header: X-API-Key: SecureAI2024_TextAnalytics_789xyz

POST /analyze-sentiment
- Body: {"text": "your text here"}
- Returns sentiment analysis

POST /detect-language  
- Body: {"text": "your text here"}
- Returns language detection

POST /extract-keywords
- Body: {"text": "your text here", "max_keywords": 10}
- Returns keyword extraction

POST /summarize-text
- Body: {"text": "long text here", "max_length": 150, "min_length": 30}
- Returns text summary

POST /generate-content
- Body: {"prompt": "topic", "content_type": "blog_post", "tone": "professional", "max_length": 300}
- Returns generated content

POST /detect-ai-content
- Body: {"text": "text to analyze"}
- Returns AI detection results

POST /analyze-readability
- Body: {"text": "your text here"}
- Returns readability metrics

POST /generate-meta-description
- Body: {"content": "content text", "max_length": 160}
- Returns SEO meta description

🛠️ TROUBLESHOOTING:

Problem: "Module not found" errors
Solution: Re-run pip install command

Problem: "Permission denied" 
Solution: Run PowerShell as Administrator

Problem: Port 8000 already in use
Solution: Use different port: uvicorn main:app --port 8001

Problem: API key authentication fails
Solution: Check your .env file has correct API key

Problem: TextBlob errors
Solution: Re-run nltk downloads:
python -c "import nltk; nltk.download('punkt'); nltk.download('brown')"

🔍 TESTING CHECKLIST:

□ Python version check (python --version)
□ All dependencies installed (no import errors)
□ .env file configured with API key
□ NLTK data downloaded
□ API starts without errors
□ Health endpoint responds
□ At least one AI endpoint works with API key
□ Interactive docs accessible at /docs

📊 SUCCESS INDICATORS:

✅ API starts with message: "Uvicorn running on http://0.0.0.0:8000"
✅ Health check returns: {"status": "healthy", "message": "API is running"}
✅ Sentiment analysis returns proper JSON with sentiment, confidence, scores
✅ API docs page loads at http://localhost:8000/docs
✅ No Python import errors when starting

🎯 NEXT STEPS AFTER TESTING:

1. Run comprehensive test suite: python test_api.py
2. Deploy to cloud platform (Railway, Heroku, etc.)
3. Register on RapidAPI as provider
4. List your API on RapidAPI marketplace
5. Start earning money! 💰

💡 TIPS:
- Keep the terminal window open while API is running
- Use Ctrl+C to stop the API server
- Check http://localhost:8000/docs for interactive API testing
- Your API key is: SecureAI2024_TextAnalytics_789xyz (save this!)

🆘 SUPPORT:
If you encounter issues:
1. Check this setup guide again
2. Verify all dependencies are installed
3. Check Python and pip versions
4. Try running: pip install --upgrade pip
5. Restart your terminal/PowerShell

===============================================
Your AI API is ready to make money on RapidAPI!
===============================================
